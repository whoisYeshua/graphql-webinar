import { urlBuilder } from './config/urlBuilder'

export const continentsUrl = urlBuilder('continents')
export const countriesUrl = urlBuilder('countries')
export const languagesUrl = urlBuilder('languages')
